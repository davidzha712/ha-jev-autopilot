name: HACS Action

on:
  push:
  pull_request:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  hacs:
    name: HACS Action
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # No ignore key. HACS requires this to pass without ignores before the
      # default-list PR is considered at all.
      - uses: hacs/action@main
        with:
          category: integration
